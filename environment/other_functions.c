#include "main.h"
#include <stdio.h>

/**
 * _strlen - calculates length of string 
 *@s: string
 *
 * Return: Always 0.
 */
int _strlen(const char *s)
{
int i;
for (i = 0; s[i] != '\0'  ; i++)
{
}

return (i);
}
